class User {
    constructor(id, firstName, lastName, emailId, mobileNumber) {
        this.id = id;
        this.firstName = firstName;
        this.lastName = lastName;
        this.emailId = emailId;
        this.mobileNumber = mobileNumber
    }
}

export default User;